/* const Product = require("../models/Product.model");

module.exports.list = (req, res, next) => {
  Product.find()
    populate('business')
    populate('like')
    .then((products) => {
      res.json(products);
    })
    .catch((e) => next(e));
}; */
